<header class="navbar navbar-fixed-top navbar-inverse">
  <%= link_to t("navigation.sample_app"), root_path, id: "logo" %>
  <div class="container">
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <%= link_to "#", class: "dropdown-toggle", data: {toggle: "dropdown"} do %>
              <%= t("navigation.account") %>
              <b class="caret"></b>
            <% end %>
            <ul class="dropdown-menu">
              <li><%= link_to t("navigation.profile"), user_path(current_user) %></li>
              <li><%= link_to t("navigation.setting"), edit_user_path(current_user) %></li>
              <li class="divider"></li>
              <li><%= link_to t("navigation.logout"), destroy_user_session_path, data: {"turbo-method": :delete} %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to t("navigation.log_in"), new_user_session_path %></li>
        <% end %>
        <li class="language-switcher">
          <a href="#" class="language-toggle">
            <%= I18n.locale.to_s.upcase %> <span class="caret"></span>
          </a>
          <ul class="language-menu">
            <li><%= link_to t("locales.vi"), url_for(locale: :vi) %></li>
            <li><%= link_to t("locales.en"), url_for(locale: :en) %></li>
          </ul>
        </li>
      </ul>
    </nav>
  </div>
</header>
