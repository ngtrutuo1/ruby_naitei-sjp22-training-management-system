<% provide(:title, t(".title")) %>
<div class="container my-4">
  <div class="row mb-4">
    <div class="col-xs-6 text-left">
      <h4 class="m-0"><i class="bi bi-person-lines-fill me-2"></i><%= t(".manager_header") %></h4>
    </div>
  </div>

  <%= render "filter" %>
  <%= render "shared/reports_table" %>
</div>
