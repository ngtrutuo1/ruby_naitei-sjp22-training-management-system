<tr>
  <td><%= task.name %></td>
  <td><%= task.taskable_name %></td>
  <td>
    <%= link_to t(".detail"), supervisor_task_path(task), class: "btn btn-info btn-sm" %>
    <%= link_to t(".delete"), supervisor_task_path(task), data: {turbo_method: :delete, turbo_confirm: t(".confirm_delete")}, class: "btn btn-danger btn-sm" %>
  </td>
</tr>
